// This layout is a pass-through for now but can be used for nested settings navigation.
// It inherits the main AdminLayout.
export default function AdminSettingsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
